/*void recebeValores(Serial Porta) {
  
  while (Porta.available() > 0) 
  myString = Porta.readStringUntil('\n');

  if(myString != null){ //garante que não haja "lixo" na variável
  String data [] = split(myString, "101");
  data[1] = "";
  click++;
}
}*/
